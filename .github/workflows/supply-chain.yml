name: Supply chain

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  schedule:
    - cron: "17 3 * * 1"

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with: {persist-credentials: false}
      - run: rustup toolchain install 1.95.0 --profile minimal --no-self-update
      - run: cargo +1.95.0 install cargo-deny --version 0.18.9 --locked
      - run: cargo +1.95.0 deny check advisories bans licenses sources
      - run: cargo +1.95.0 install cargo-audit --version 0.22.0 --locked
      - run: cargo +1.95.0 audit --no-yanked
